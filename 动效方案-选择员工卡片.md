# 动效方案 · 选择员工（卡片网格 + Morph 展开）

> 适用页面：开单 / 办卡 详情中的「选择员工」底部 Sheet（`billStaffMask` / `addCardStaffMask`）
> 原型源：`card/demo.html`（员工卡片网格选择交互）
> 目标：前端开发 100% 还原原型 UI UX 动效。本文档所有参数、缓动、时序均取自原型实际代码，可直接对照实现。

---

## 1. 交互链路总览（状态机）

```
┌─ 入口行（详情页 / 办卡表单）点击「选择员工」
│
▼
[底部 Sheet 打开]
│
├─ 员工卡片网格（3 列，可多选）─────────────── 状态 IDLE
│     │
│     │ 点击任意卡片
│     ▼
│   [Morph 展开] ─────────────────────────── 状态 EDIT
│   （被点卡片 收窄→展开；其余卡片变暗；出现 scrim 遮罩）
│     │
│     ├─ 点「点客 / 散客」───────────────────────（平均提成模式）→ 完成
│     ├─ 点「点客 / 散客」→ 面板切换为工位选择 ────（按工位模式）
│     │      └─ 点「小工 / 中工 / 大工」→ 完成
│     └─ 点 scrim 遮罩 → 取消，网格一次复原
│
▼
[卡片回到网格：打勾 + 红色摘要 + 弹跳] ──────── 状态 DONE（可继续点其他卡片）
│
└─ 点「完成」/ 点遮罩 ── Sheet 关闭，入口回填「已选 N 人」+ 摘要行列表
```

**两种提成模式的差异**（决定二级选择的面板深度）：

| 模式 | 点击卡片后流程 |
|---|---|
| 平均提成（`avg`） | 点「点客/散客」二选一即完成 |
| 按工位（`station`） | 先点「点客/散客」，再点「小工/中工/大工」选工位，才完成 |

提示文案随模式变化（见 §2）。

---

## 2. 底部 Sheet 规格

### 2.1 结构

```
.picker-mask.picker-mask--bottom   （全屏遮罩）
└─ .picker-sheet.tall              （白色面板）
   ├─ .picker-head                 「选择员工」
   ├─ .add-card-staff-sheet__hint   提示文案
   ├─ .add-card-staff-sheet__body   卡片网格容器（可滚动）
   └─ .picker-foot                  「完成」按钮
```

### 2.2 数值规格

| 元素 | 属性 | 值 |
|---|---|---|
| 遮罩 | 背景 / z-index | `rgba(0,0,0,.45)`；通用 40，`#billStaffMask` 42 |
| Sheet | 位置 / 圆角 | 底部对齐，仅顶部圆角 `16px 16px 0 0` |
| Sheet | 最大高度 | `max-height: 85%` |
| Sheet | 背景 | `#fff` |
| 标题 | 字号 / 字重 / 对齐 | `17px` / `600` / 居中，`padding: 20px 20px 0` |
| 提示 | 字号 / 色 | `12px` / `var(--text-sec)`，`margin: 0 16px 10px` |
| 内容容器 | 外边距 / 内边距 / 背景 | `margin: 0 16px 16px; padding: 12px`，`#FAFAFA`，圆角 `12px` |
| 内容容器 | 高度约束 | `max-height: min(52vh, 420px)`，`overflow-y: auto` |
| 完成按钮 | 尺寸 | 全宽，高 `48px`，圆角 `12px`，`16px / w500` |
| 按钮脚 | 内边距 | `padding: 8px 20px 40px` |

### 2.3 提示文案（随模式切换）

- 平均提成：`可多选员工；点卡片后选择点客或散客。`
- 按工位：`可多选员工；点卡片后选择点客/散客，再选择工位（小工/中工/大工）。`

### 2.4 进出场

原型当前为**瞬时显隐**（`display: none ↔ flex`，无淡入淡出、无滑动）。还原时保持瞬时即可；若后续需要增强，可复用原型中 `cartSheet` 的进场动画：

```css
animation: slideUp .25s ease;
@keyframes slideUp { from { transform: translateY(100%); } to { transform: translateY(0); } }
```

---

## 3. 卡片网格静态规格

```
.staff-grid  （display: grid，3 列）
├─ grid-template-columns: repeat(3, minmax(0, 1fr))
├─ gap: 10px 8px        ← 行距 10 / 列距 8
├─ padding: 2px 0 12px
└─ .staff-card          （min-height 96px，will-change: transform, width）
   └─ .staff-card__panel（白卡）
      ├─ 头像   40px 圆形（无图 = 姓名首字 + 品牌色底）
      ├─ 姓名   12px / w500，单行省略
      └─ 职位   10px / 次要色，单行省略
```

卡片面板基础样式：

```css
.staff-card__panel {
  border-radius: 12px;
  background: #fff;
  border: 1px solid #E8E8E8;
  padding: 10px 8px 8px;
  display: flex; flex-direction: column; align-items: center;
  justify-content: center; gap: 6px;
  transition: border-color .22s ease, background .22s ease,
              box-shadow .28s cubic-bezier(.22,.82,.24,1);
}
.staff-card__panel:active { transform: scale(.97); }
```

> 列号由渲染索引决定：`index % 3` → `0` 左列 / `1` 中列 / `2` 右列。Morph 展开位移依赖此列号。

---

## 4. 卡片三种状态

### 4.1 未选中（默认）
白卡 + 灰色职位行。按下 `scale(.97)`。

### 4.2 已选中（DONE）

| 元素 | 规格 |
|---|---|
| 面板 | `linear-gradient(180deg, #FFF8F8, #FFF2F2)`，边框 `#FFD5D9` |
| 头像描边 | `#FFE0E3`（2px 环） |
| 勾选徽标 | 左上角 `top:6px; left:6px`，16px 圆形，品牌红底、白色对勾（SVG 10px），`pointer-events:none` |
| 摘要行 | 「点客·大工」`11px / w600 / 品牌红` |
| 清除按钮 | 右上角 `top:-6px; right:-6px`，22px 圆形，白底、灰叉 `#929292`、边框 `#E8E8E8`，投影 `0 2px 8px rgba(0,0,0,.08)`，`:active` `scale(.92)`；编辑态隐藏 |

摘要行文案规则（`staffPickSummaryText`）：

```
平均提成模式：  「点客」或「散客」
按工位模式：    「点客·大工」/「散客·中工」等（点客散客 + 工位，用 · 连接）
```

### 4.3 变暗（编辑态下的非当前卡）

```css
.staff-card.is-dim {
  opacity: .36; pointer-events: none; filter: saturate(.85);
}
```

---

## 5. Morph 展开动画（核心）

### 5.1 执行时序

```
点击卡片
  → 整体重渲染网格（编辑卡 .is-editing，其余卡 .is-dim，网格 .is-morphing，出现 scrim 遮罩）
  → requestAnimationFrame 双帧触发动画：

  ① 收窄（.is-pinching） 0.09s
     被点卡片：宽度 cellW → 32px，水平位移 0 → narrowDx
     面板保留白卡壳（边框/圆角/阴影），按钮组 opacity:0、hidden
     同行两邻卡保持原位不动

  ② 展开（.is-expanding） 0.17s
     被点卡片：宽度 32px → editW
     两邻卡：translateX 同步让位（同 0.17s 同缓动）
     面板清除卡壳（border:none / border-radius:0 / background:transparent）
     按钮组 opacity:0 → 1（delay 0.03s）
```

> ② 由 `transitionend` 触发，`setTimeout(…, 110)` 兜底。两个 class `.is-pinching` / `.is-expanding` 二选一推进，禁止并存。

### 5.2 尺寸计算（照抄公式）

```
gap       = 8                          // 列距
cellW     = (gridW − gap×2) / 3        // 单格宽
editWFull = face === 'role'
              ? gridW                  // 工位面板：整行全宽
              : min(gridW, cellW×2 + gap)   // 点客/散客面板
editW     = max(cellW, round2(editWFull × 2/3))   // 展开宽 = 原宽 2/3
narrowW   = 32                         // 收窄窄条宽
narrowDx  = (cellW − narrowW) / 2      // 收窄水平居中位移
```

**列位移（同行 3 卡最终布局，左→右）**：

| 被点列 | 左列 `lefts` | 中列 `lefts` | 右列 `lefts` |
|---|---|---|---|
| 左列 (col 0) | `0` | `editW + gap` | `editW + gap×2 + cellW` |
| 中列 (col 1) | `lefts[1] − gap − cellW` | `(gridW − editW)/2` | `lefts[1] + editW + gap` |
| 右列 (col 2) | `lefts[1] − gap − cellW` | `lefts[2] − gap − cellW` | `gridW − editW` |

> 卡宽 `width` + 位移 `transform: translateX` 同时过渡；编辑卡 `z-index: 6`。

### 5.3 过渡曲线（必须精确）

| 阶段 | 属性 | 时长 | 缓动 |
|---|---|---|---|
| ① 收窄 | transform, width | `0.09s` | `cubic-bezier(.4, 0, .2, 1)` |
| ② 展开 | transform, width | `0.17s` | `cubic-bezier(.22, .82, .24, 1)` |
| 按钮组淡入 | opacity | `0.11s ease 0.03s` | `ease` |

### 5.4 编辑面板（展开后的按钮组）

- 面板：高 `96px`，无连体壳，`padding:0; gap:0`，独立圆角按钮
- **点客/散客**（2 按钮）：

| | 点客 | 散客 |
|---|---|---|
| 文字色 | `#C62828` | `#4A667A` |
| 背景 | `linear-gradient(180deg,#FFF9F9,#FFF2F2)` | `linear-gradient(180deg,#F8FAFC,#F1F5F8)` |
| 边框 | `rgba(243,47,65,.18)` | `rgba(74,102,122,.16)` |

- **工位**（3 按钮，顺序：小工 → 中工 → 大工）：

| | 小工 | 中工 | 大工 |
|---|---|---|---|
| 文字 / 边框色 | `#6B8F71` | `#6A7F96` | `#C62828` |

按钮通用：

```css
.staff-card__split-btn {
  flex: 1 1 0; min-width: 0;
  border-radius: 10px; background: #fff;
  font-size: 15px; font-weight: 650;    /* 工位模式：14px / 600 */
  box-shadow: 0 2px 8px rgba(26,26,26,.06);
  transition: background .18s ease, color .18s ease,
              transform .14s ease, box-shadow .18s ease;
}
.staff-card__split-btn:active {
  transform: scale(.97);
  box-shadow: 0 1px 4px rgba(26,26,26,.08);
}
```

### 5.5 scrim 遮罩

编辑态下出现，覆盖卡片区块：

```css
.staff-card-scrim {
  position: absolute; inset: -8px -4px -12px; z-index: 1;
  background: rgba(247, 247, 247, .42); border-radius: 12px;
  cursor: pointer;   /* 点击 = 取消编辑 */
}
```

编辑态网格层：`.staff-grid` 加 `pointer-events: none`（编辑卡自身 `pointer-events: auto`），保证点击只落在 scrim 或按钮上。

---

## 6. 完成 / 取消动画

### 6.1 完成（选中成功）

点击按钮 → `navigator.vibrate(8)`（8ms 触觉反馈）→ 写入状态 → 网格复原重渲染，三个动画**同时**播放：

| 动画 | 时长 / 缓动 | 关键帧 |
|---|---|---|
| `staffDonePop` 卡片弹跳 | `0.42s cubic-bezier(.22,.82,.24,1)` | `0% scale(.96) → 55% scale(1.03) → 100% scale(1)` |
| `staffCheckIn` 勾标弹入 | `0.38s cubic-bezier(.22,.82,.24,1)` | `0% scale(.4) opacity 0 → 70% scale(1.12) → 100% scale(1) opacity 1` |
| `staffMetaIn` 摘要上浮 | `0.36s cubic-bezier(.22,.82,.24,1)` | `0% translateY(4px) opacity 0 → 100% translateY(0)` |

### 6.2 取消（点 scrim）

清除编辑态 → 网格**一次复原**，不做收窄两段式（直接回到各卡片原始位置 + 原始透明度）。

### 6.3 关闭 Sheet

点「完成」/ 点遮罩 → Sheet 瞬时收起；详情入口回填：

- 入口值：「未选择」→ **「已选 N 人」**（品牌红 `w500`，`13px`）
- 入口下方新增摘要行列表（`detail-staff-summary`）：
  - 白卡行（`#fff`，边框 `#EDEDED`，圆角 10px，`padding 10px 12px`）
  - 内容：姓名（14px/w600）+ 职位（13px 次要色）+ 摘要「点客·大工」（13px 次要色，省略号）
  - 尾部删除按钮（28px 圆形灰叉，`:active` 变灰底）→ 点击即时移除 + 8ms 震动

---

## 7. 动效参数速查表

| 参数 | 值 |
|---|---|
| 主缓动（弹性） | `cubic-bezier(.22,.82,.24,1)` |
| 收窄缓动 | `cubic-bezier(.4,0,.2,1)` |
| 收窄时长 | `0.09s` |
| 展开时长 | `0.17s` |
| 按钮组淡入 | `0.11s ease 0.03s` |
| 卡片弹跳 | `0.42s` |
| 勾标弹入 | `0.38s` |
| 摘要上浮 | `0.36s` |
| 面板常态过渡 | 边框/背景 `0.22s ease`，阴影 `0.28s` |
| 按钮常态过渡 | 背景/颜色/阴影 `0.18s`，transform `0.14s` |
| 按下反馈 | 卡片 `scale(.97)`；清除钮 `scale(.92)` |
| 触觉反馈 | `navigator.vibrate(8)` |
| 展开宽 | designate = 原宽 2/3；role = 整行全宽 |
| 收窄窄条宽 | `32px` |
| 网格 | 3 列，gap 行 10 / 列 8 |
| 编辑卡 z-index | `6`；scrim `1`；遮罩 `40 / 42` |
| 卡片最小高 | `96px` |

---

## 8. 无障碍（prefers-reduced-motion）

系统开启「减弱动态效果」时，**禁用全部卡片动画**：

```css
@media (prefers-reduced-motion: reduce) {
  .staff-card.is-editing .staff-card__panel { animation: none !important; }
  .staff-card.is-done .staff-card__panel    { animation: none !important; }
  .staff-card { transition: none !important; }
  .staff-card__panel { transition: none; }
  .staff-card__split-btn { transition: none !important; }
  .staff-card.is-expanding .staff-card__split { transition: none !important; }
}
```

逻辑侧同样判断 `matchMedia('(prefers-reduced-motion: reduce)')`，命中时直接 `applyFinalLayout()` 跳转最终状态，不播放两段式。

---

## 9. 实现要点 Checklist（防偏差）

1. **先渲染后动画**：Morph 依赖 `requestAnimationFrame` 双帧（布局刷新后）再启动 transition。
2. **只动画 `transform` / `width`**：禁止用 `height` / `top` 模拟，否则投影、圆角会跳变。
3. **尺寸按公式算**：`editW = max(cellW, editWFull × 2/3)`，列位移用 `gap=8` 精确推导，保证同行三卡紧邻不重叠、不产生多余留白。
4. **邻卡与编辑卡同速让位**：收窄阶段邻卡不动；展开阶段邻卡与编辑卡同用 `0.17s` 同缓动。
5. **`.is-pinching` / `.is-expanding` 互斥推进**：靠 `transitionend` + `110ms` 定时双保险。
6. **展开后清卡壳**：面板 `border:none / border-radius:0 / background:transparent`，仅保留独立圆角按钮 —— 这是「无连体壳」观感的关键。
7. **完成动画三合一**：`is-done` 同时触发弹跳 + 勾标 + 摘要，且网格一次复原（不再做二次收窄）。
8. **触觉反馈范围**：仅 选中 / 删除 / 切换 动作触发 8ms 震动；打开 / 关闭 Sheet 不震动。
9. **状态数据同步**：每个 item 维护 `staffIds`、`staffRoles`、`staffDesignated` 三个字段；工位模式校验「已选员工必须都有工位」才能完成。
10. **平均 / 按工位双模式**：卡片二级面板深度与提示文案都随 `getAchCalcModeForKind(kind)` 切换。

---

## 10. 还原验收对照表

| 检查点 | 预期 |
|---|---|
| 打开 Sheet | 卡片网格 3 列整齐排布，白卡 + 头像 + 姓名 + 职位 |
| 点击卡片 | 该卡收窄成窄条 → 同行邻卡让位、卡片展开露出双按钮，其余卡片变暗 |
| 点「散客」 | 卡片复位 + 弹跳 + 左上角红勾 + 红色「散客」摘要 + 8ms 震动 |
| 点「点客」（按工位模式） | 面板翻转为三工位按钮 |
| 点「大工」 | 完成，卡片显示「点客·大工」摘要 |
| 点 scrim | 立即取消，网格一次复原 |
| 点卡片右上角 × | 移除该员工，摘要行消失 |
| 已选 2 人 | 两个卡片均有红勾，摘要行各显示各自点客/散客·工位 |
| 点「完成」 | Sheet 关闭，入口显示「已选 2 人」（品牌红）+ 摘要行列表 |
| 减弱动态效果 | 全部动画禁用，仅瞬时状态切换 |
