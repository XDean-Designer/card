# PRD：会员卡管理（商户端）

| 字段 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 日期 | 2026-07-22 |
| 交互原型（唯一可视依据） | 同目录 `demo.html`（中文镜像：`剑琅联盟-会员卡-权益组合版.html`） |
| 画布 | 390 × 844 |
| 目的 | 供前端 / 后端 / 测试及 AI 工具落地「会员卡管理」；**只需本文 + 交互原型**，无需其他说明文档 |

---

## 怎么读这份 PRD

正文按模块编排。**不用通读全文**，按角色跳章即可。

| # | 模块 | 前端 | 后端 | 测试 | 说明 |
|---|------|:----:|:----:|:----:|------|
| 1 | 背景 / 目标 / 范围 / 能力映射 | ✓ | ✓ | ✓ | 防范围蔓延 |
| 2 | 用户场景与成功标准 | ✓ | 了解 | ✓ | 测「业务对不对」 |
| 3 | 信息架构与页面清单 | ✓ | 了解 | ✓ | 做哪些页 |
| 4 | 状态机 | ✓ | **必看** | **必看** | 在售/下架、锁编辑、表单模式 |
| 5 | 交互细则 / 异常流 | ✓ | 了解 | ✓ | 校验、toast、拦截 |
| 6 | 功能规格 | ✓ | 了解 | ✓ | 逐屏字段与跳转 |
| 7 | 数据模型与接口约定 | ✓ | **必看** | ✓ | 字段、能力清单、时序 |
| 8 | 业务规则与算例 | ✓ | **必看** | ✓ | 权益、策略、退卡 |
| 9 | 文案清单与权限 | ✓ | 部分 | ✓ | 文案；权限占位 |
| 10 | 验收 P0 + 造数场景 | 了解 | 了解 | **必看** | Given→When→Then |
| 11 | 非功能（极简） | ✓ | ✓ | ✓ | |
| 12 | 交付物 | ✓ | — | ✓ | 仅 PRD + 原型 |

**建议阅读路径**

- **前端**：1 → 3 → 4 → 5 → 6 → 9 → 12（联调再看 7、8）
- **后端**：1 → 4 → 7 → 8（其余了解）
- **测试**：1 → 2 → 4 → 8 → 10（对照 5、6、原型走查）

**先读这几条（贯穿全文）**

1. 新系统用 **权益组合**（面值 / 项目 / 折扣）建卡，**不再选「五种卡类型」**；整套取代旧会员卡系统。  
2. 创建固定 **三步**：基本信息 → 权益组合 → 用卡策略；Step2 **至少开启并配置一类权益**。  
3. **当前有效持卡数 > 0** 时模板 **不可编辑**（可复制建卡、可下架、可办卡/退卡/延期）。  
4. **已下架**不可新办；已持卡仍可退卡/延期；可重新上架或复制建卡。  
5. 退卡为 **整卡退**；建议退款可按策略计算，**实际退款金额可改**后确认。  
6. 用卡策略两轨独立：`消耗及退卡计算规则`、`业绩计算规则`，各可选均值或减值/原价。  
7. 接口路径/字段名以联调为准；本文给 **能力与规则**，标 `【待联调确认】` 处不臆造现网 URL。

交互行为与文案以 **`demo.html` 当前表现** 为准；本文把规则与验收写全，便于无历史上下文的机器与人直接开工。

---

# 模块 1　背景 / 目标 / 范围 / 能力映射
> 读者：前端 ✓ · 后端 ✓ · 测试 ✓

## 1.1 背景

原先的会员卡系统将卡划分为 **5 种固定类型**，每种类型套用 **预设模板字段**。商户创建会员卡时：

- 只能在类型预设内填字段，**自由度不足**；
- 每次都要面对 **繁复的设置页** 逐项填充，创建成本高。

本期交付的「会员卡管理」将 **整套取代** 上述旧系统（非长期并行两套产品线），对字段按能力重新解构。

## 1.2 目标

| 目标 | 说明 |
|------|------|
| 取代旧系统 | 新建模与流程成为唯一建卡体系 |
| 能力覆盖 | 可配置出与旧五类 **功能等价** 的卡，也可配置旧体系 **无法做出** 的组合卡 |
| 创建更简 | 三步完成：基本信息 → 权益组合 → 用卡策略 |
| 运营闭环 | 列表 / 详情 / 上下架 / 复制 / 办卡 / 延期 / 整卡退卡 |
| 策略可配置 | 退卡消耗与员工业绩规则在建卡时选定并随模板保存 |

## 1.3 本期做（In）

- 会员卡模板：创建、编辑（无持卡时）、复制建卡、在售/已下架列表与详情  
- 权益组合：面值、项目（每项/共计、购买/赠送/不限次）、折扣（比例/固定金额）  
- 卡面主题色（5 色）、卡有效期限（日/月/年/永久，受不限次约束）  
- 用卡策略：消耗及退卡计算规则、业绩计算规则  
- 办卡（多选既存会员）、办卡成功  
- 已持卡：延期、整卡退卡（估值 + 实退可改）  
- 上下架及文案提示  

## 1.4 本期不做（Out）

- **开单结账**全链路（选客、开单台、支付等）— 不在本文范围  
- **项目管理（价目表）**作为独立产品交付（建卡时「选项目」依赖价目数据源，见依赖说明）  
- **会员管理**（会员 CRUD；办卡仅从既存会员列表多选）  
- 部分退卡、劳动业绩独立运算台、预约联调  
- 原型工具栏「无数据/有数据」造数按钮 — **仅演示**，不进正式产品  

## 1.5 旧能力 ↔ 新组合权益（能力映射，不写废弃产品名）

| 旧体系常见能力（语义） | 新体系如何配置 |
|------------------------|----------------|
| 储值/充值类（买赠面值） | 开启 **面值**：填面值金额；赠送 = 面值 − 购买金额（自动） |
| 次卡/项目次数类 | 开启 **项目**：选适用项目；每项或共计；购买/赠送次数或不定次 |
| 折扣/会员价类 | 开启 **折扣**：选适用项目；标尺折扣或固定金额 |
| 期限卡 | Step1 **有效期限**；含不限次项目时不可为「永久」 |
| 组合（储值+项目+折扣等） | Step2 **同时开启多类权益** 并分别配置 |
| 旧类型无法覆盖的组合 | 任意权益开关组合 + 独立用卡策略 |

映射不足时 **以新字段与原型行为为准**，不编造旧系统字段表。

---

# 模块 2　用户场景与成功标准
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

## 2.1 角色

| 角色 | 说明 |
|------|------|
| 店主（默认） | 可进行本文全部操作 |
| 店员 | **【待产品确认】** 本期原型未表现差异；权限见模块 9.2 占位 |

## 2.2 核心场景

| 场景 | 用户期望 |
|------|----------|
| 首次建卡 | 三步填完保存 → 上架 → 可立即办卡 |
| 配置组合权益 | 面值/项目/折扣按需勾选配置，预览卡面 |
| 有持卡后改规则 | 不能直接编辑模板；复制建卡后改新卡 |
| 停售 | 下架后新客不能办；已持卡不受影响 |
| 再开售 | 重新上架后可再办 |
| 为会员办卡 | 选未持该卡的会员，可多选，办卡成功 |
| 延期 | 对非永久有效持卡设置延期时长（可含参考费用） |
| 退卡 | 看到建议退款与明细，改实际退款后确认整卡退 |

## 2.3 成功标准（可判定）

1. 空店可创建至少一张在售卡并出现在「在售」列表。  
2. Step2 未配置任何权益内容时 **不能** 进入 Step3 / 不能保存成功。  
3. `当前有效持卡 > 0` 时详情「编辑」不可用，并提示复制建卡。  
4. 已下架卡列表无「办卡」入口；详情可「已持卡管理」（退卡/延期）。  
5. 办卡后持卡人数/办卡次数等运营数据按规则增加（见原型统计逻辑）。  
6. 退卡确认后该会员不再作为有效持卡；实退金额以确认值为准写入结果提示。

---

# 模块 3　信息架构与页面清单
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

```
会员卡管理
 ├─ 列表（在售 / 已下架）
 ├─ 添加/编辑/复制 · Step1 基本信息
 ├─ Step2 权益组合
 │    ├─ 添加/编辑项目权益
 │    └─ 添加/编辑折扣权益
 ├─ Step3 用卡策略
 ├─ 创建成功
 ├─ 会员卡详情
 ├─ 办卡成功
 └─ Sheets
      ├─ 选择会员（办卡 | 退卡/延期）
      ├─ 退卡估值
      ├─ 下架确认 / 重新上架确认
      └─ 辅助：金额键盘、计次说明、运营数据说明、不限次改有效期、策略明细等
```

| 页面/层 | 导航标题（原型） | 说明 |
|---------|------------------|------|
| 列表 | 会员卡管理 | Tab：在售 / 已下架；底栏「添加会员卡」 |
| Step1 | 添加会员卡 / 编辑会员卡 | 基本信息 + 卡面模板 |
| Step2 | 权益组合 | 三类权益 Switch + 配置 |
| Step3 | 用卡策略 | 退卡规则 + 业绩规则；保存 |
| 创建成功 | — | 返回列表 / 立即办卡 |
| 详情 | 会员卡详情 | 运营数据 + 卡面信息 + 操作 |
| 项目权益页 | 添加/编辑项目权益 | 选项目 + 计次参数 |
| 折扣权益页 | 添加/编辑折扣权益 | 选项目 + 折扣参数 |
| 办卡成功 | — | 返回列表 / 继续办卡 |
| 选择会员 | 选择会员 | Tab 办卡 / 退卡/延期 |
| 退卡估值 | 退卡估值 | 建议退款 + 实退 |
| 下架/上架 | 下架会员卡 / 重新上架 | 确认对话框 |

实现路由建议（名称可调整）：`card-list` / `card-step1` / `card-step2` / `card-step3` / `card-create-success` / `card-detail` / `card-project-benefit` / `card-discount-benefit` / `card-issue-success` + sheets。

---

# 模块 4　状态机
> 读者：前端 ✓ · 后端 **必看** · 测试 **必看**

## 4.1 模板上架状态

| 状态 | 产品表现 | 含义 |
|------|----------|------|
| 在售 `shelved=false` | 「在售」列表；可办卡；详情可下架/编辑（未锁时） | 新客可办理 |
| 已下架 `shelved=true` | 「已下架」列表；不可新办；详情可重新上架、复制、已持卡管理 | 停售 |

```
在售 ──确认下架──► 已下架 ──确认上架──► 在售
```

- 下架：**不影响**已持卡客户。  
- 已下架：选择会员 Sheet **仅「退卡/延期」**（隐藏办卡 Tab）。

## 4.2 模板编辑锁定

| 条件 | 结果 |
|------|------|
| `当前有效持卡数 validCardCount == 0` | 可进入编辑三步并保存覆盖原模板 |
| `validCardCount > 0` | **不可编辑**；详情「编辑」禁用；提示复制建卡 |

```
无持卡 ──编辑──► 三步表单 ──保存修改──► 更新原模板 → 回详情
有持卡 ──点编辑──► 拦截（toast）──► 引导复制建卡 → 新建模板
```

## 4.3 表单模式 `formMode`

| 模式 | 入口 | 保存结果 |
|------|------|----------|
| `create` | 「添加会员卡」/空态 | 新建在售模板 → 创建成功页 |
| `edit` | 详情「编辑」（未锁定） | 更新原模板 → 详情；toast「已保存」 |
| `duplicate` | 「+复制建卡」 | 新建模板（名称默认「原名（副本）」）；原卡不变 → 创建成功页 |

复制模式三步均展示横幅：`正在基于「{原卡名}」创建新卡，保存后原卡不受影响`。

## 4.4 列表 Tab

`listShelfTab`: `active` | `shelved`，仅切换列表数据源与空态文案，不改模板状态。

## 4.5 持卡相关（实例层）

| 概念 | 说明 |
|------|------|
| 未持卡 | 办卡 Tab 可选 |
| 已持卡（有效） | 退卡/延期 Tab；可退卡；非永久可延期 |
| 已失效 | 展示「已失效」；操作以原型表现为准 |
| 永久有效 | 延期入口提示无需延期 |

---

# 模块 5　交互细则 / 异常流
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

## 5.1 总原则

1. 列表点卡面进详情；footer 按钮不冒泡进详情。  
2. 创建/编辑/复制均为三步；Step1/2「下一步」、Step3「保存/保存修改」。  
3. Step2 必须至少有一类已开启且内容有效的权益。  
4. 金额类输入使用原型金额键盘交互（正式产品对齐现网数字键盘即可）。  
5. 所有拦截以 toast 或对话框给出 **明确中文原因**（见模块 9）。

## 5.2 离开与返回

- 各步标题栏返回：回到上一步或列表（与原型一致：Step1 回列表，Step2→1，Step3→2）。  
- Sheet 点遮罩关闭：取消当前 sheet，不提交。  
- **【待联调确认】** 三步中途系统返回是否弹「放弃编辑」：原型未做拦截时，正式产品可按现网统一规范补齐。

## 5.3 校验失败（须拦截前进/保存）

| 时机 | 条件 | 提示（原型文案） |
|------|------|------------------|
| Step1 下一步 | 卡名称为空 | 请填写卡名称 |
| Step1 下一步 | 购买金额为空 | 请填写购买金额 |
| Step1/相关 | 有效期限未设妥 | 请设置有效期限 |
| Step2 下一步 | 未选任何权益内容 | 请至少选一项权益内容 |
| Step2 | 开启面值但未填面值 | 请填写面值金额 |
| Step2 | 开启项目但无已保存项目权益 | 请至少保存一个项目权益 |
| Step2 | 开启折扣但无已保存折扣权益 | 请至少保存一个折扣权益 |
| 项目权益确定 | 未选项目 | 请先选择项目范围 |
| 项目权益确定 | 未设次数且非不限次 | 请设置次数或选择不限次 |
| 折扣确定 | 未选项目 | 请先选择折扣范围 |
| 折扣固定金额 | ≤0 或空 | 请输入大于 0 的固定金额 |
| 折扣比例 | 未设置 | 请设置折扣 |
| 编辑锁定 | 有持卡仍保存/进编辑 | 已有持卡，不可编辑；请复制建卡后修改 |
| 不限次 + 永久 | 冲突 | 弹窗改有效期，或 toast：不限次项目须先设置卡有效期（不可为永久） |
| 办卡确定 | 未选会员 | 请至少选择一位会员 |
| 延期 | 永久卡 | 该卡永久有效，无需延期 |
| 延期 | 未设时长 | 请设置延期时长 |
| 延期 | 参考费用为负 | 参考费用不能为负数 |
| 退卡 | 实退为空 | 请填写实际退款金额 |

## 5.4 空态

| 位置 | 文案 |
|------|------|
| 在售空 | 标题「暂无会员卡」；说明点击底部「添加会员卡」开始创建 |
| 已下架空 | 标题「暂无已下架会员卡」；说明下架后的卡会出现在这里… |
| 办卡选人空 | 所有会员均已持有该卡 |
| 卡面预览无权益 | 暂无权益信息 |

---

# 模块 6　功能规格
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

下列字段、按钮、跳转均须与原型一致实现；视觉以原型为准（品牌色 `#F32F41` 等）。

## 6.1 会员卡列表

**结构**

- 标题：会员卡管理  
- Tab：在售 | 已下架  
- 列表项：卡面条（VIP 标 + 卡名 + 价格）+ 权益摘要面板 + footer  
- 底栏：添加会员卡  

**在售 footer**：`+复制建卡`、`办卡/退卡`  
**已下架 footer**：仅 `+复制建卡`  

**卡面摘要面板**

- 面值：`面值 ¥X · 赠送 ¥Y`  
- 项目：项目名 + 次数摘要（`10次` / `10次+送1次` / `不限次`）  
- 折扣：项目名 + `N 折` 或 `¥金额`  
- 期限：有项目/折扣展示卡有效期；纯面值展示「永久有效」；否则「—」  

**跳转**

| 操作 | 结果 |
|------|------|
| 添加会员卡 | `formMode=create` → Step1 |
| 点卡面 | 详情 |
| +复制建卡 | `formMode=duplicate`，预填，卡名「原名（副本）」→ Step1 |
| 办卡/退卡 | 打开选择会员 Sheet（办卡 Tab） |

## 6.2 Step1 基本信息

| 字段 | 必填 | 说明 |
|------|:----:|------|
| 卡名称 | ✓ | 输入；placeholder「请输入」 |
| 购买金额 | ✓ | 金额；placeholder「请输入」 |
| 有效期限 | ✓ | 数量 + 单位：日 / 月 / 年 / **永久**（默认永久） |
| 卡面模板 | ✓ | 五色单选，默认品牌红；下方实时预览 |

步骤条文案：`第 1/3 步 · 基本信息`  
底栏：下一步 → Step2  

**隐藏字段**：适用对象 `audience` 默认 `['all']`，原型 Step1 **无 UI**；后端可按现网对象模型扩展，**【待联调确认】**。

## 6.3 Step2 权益组合

三个独立 Switch 区块（可多开）：

### 6.3.1 面值

- 描述：购买与赠送的面值，永久有效。  
- **面值金额**（可编）  
- **赠送**（只读）= `面值金额 − 购买金额`（按原型计算与展示格式）  

### 6.3.2 项目

- 描述：包含、赠送项目的次数  
- 已保存权益以 ticket 展示；入口 **+添加项目权益** → 项目权益页  
- ticket meta：`每项`/`共计` + 次数摘要  

### 6.3.3 折扣

- 描述：比例或定额折扣  
- ticket + **+添加折扣权益** → 折扣权益页  

底栏：下一步 → Step3（通过校验后）

## 6.4 项目权益页

- 标题：添加项目权益 / 编辑项目权益；右侧「管理项目表」（跳转价目依赖，**【待联调确认】** 是否本期可点）  
- **适用项目**：列表 Switch；全选 / 反选  
- **计次模式**：每项 | 共计  
  - 每项：每个项目各自获得相同次数；不限次时各项目独立不限次  
  - 共计：所选项目共享同一次数池；**不限次时不可选共计**  
- **权益次数 / 赠送次数 / 不限次**  
- 页内「确定」写回 Step2 ticket  

## 6.5 折扣权益页

- 适用项目：同项目页选择交互  
- **打折**（标尺：免费 ↔ 原价，展示如「8 折」）或 **固定金额**（输入 >0）  
- 「确定」写回 Step2  

## 6.6 Step3 用卡策略

| 配置项 | 选项 | 默认 |
|--------|------|------|
| 消耗及退卡计算规则 | 均值模式 / 减值模式 | 均值 |
| 业绩计算规则 | 均值模式 / 原价模式 | 均值 |

均值模式展示动态 hint（代入办卡实付、总权益面值）；`?` 打开明细说明。  
底栏：保存 / 保存修改。

## 6.7 创建成功

- 文案：会员卡已创建；已上架，可立即为会员办理  
- 展示新建卡预览  
- 按钮：返回列表 | 立即为会员办卡 → 选择会员（办卡）

## 6.8 会员卡详情

**在售顶栏操作**：下架、编辑（锁定时禁用）  
**已下架顶栏**：已下架标记、复制建卡  

**运营数据**（带「运营数据说明」）：

- 办卡总额、办卡次数、办卡人数  
- 持卡余额（仅有面值权益时）、当前有效  
- 有项目时：各项目消费次数（含赠送）、剩余次数（含赠送）；多于 4 项可展开  

**卡面信息**：面值金额/赠送、项目网格、折扣网格、项目/折扣有效期、用卡策略摘要（均值退卡·均值计绩 等，可点 `?`）

**已下架底栏**：重新上架 | 已持卡管理 → 选择会员（仅退卡/延期）

## 6.9 选择会员 Sheet

- 标题：选择会员；副标题：当前卡名  
- Tab：**办卡** | **退卡/延期**  
- 搜索：姓名或手机  
- 办卡：多选未持卡会员 → 确定 → 办卡成功  
- 退卡/延期：持卡行操作「退卡」「延期」；延期展开时长（日/月/年/永久）+ 参考费用（条件显示）+ 确认延期  

## 6.10 退卡估值 Sheet

- 副标题含会员、卡名、均值退卡/减值退卡  
- 展示：办卡实付、消耗价值、权益剩余明细、建议退款  
- **实际退款**可编辑  
- 取消 | 确认退卡  

## 6.11 下架 / 重新上架

文案见模块 9；确认后 toast「已下架」/「已重新上架」。

## 6.12 办卡成功

- 单人/多人成功描述  
- 返回列表 | 继续办卡  

---

# 模块 7　数据模型与接口约定
> 读者：前端 ✓ · 后端 **必看** · 测试 ✓

> 路径与字段名可按现网命名；下列为 **能力与规则**。标 `【待联调确认】` 的项评审/联调时拍板。

## 7.1 模板（Template）模型

| 字段 | 说明 |
|------|------|
| `id` | 模板 ID |
| `name` | 卡名称 |
| `recharge` | 购买金额（办卡实付口径的配置价） |
| `faceAmount` / `giftAmount` | 面值权益开启时的面值与赠送 |
| `benefits.balance` | bool 是否含面值 |
| `benefits.timesOrValidity` | bool 是否含项目 |
| `benefits.projectDiscount` | bool 是否含折扣 |
| `projectItems[]` | 项目权益项（见下） |
| `memberPrices{}` | 折扣规则（按项目或范围） |
| `cardColor` | `brand_red` \| `gold` \| `blue` \| `purple` \| `teal` |
| `usagePolicy.refundRule` | `alloc_pool` \| `retail` |
| `usagePolicy.performanceRule` | `alloc_pool` \| `retail` |
| `validity*` | 有效期：永久或日/月/年数量 |
| `shelved` | 是否下架 |
| `audience` | 适用对象，原型默认 `['all']` |
| `source` | `normal` \| `duplicate` \| … |
| `duplicatedFrom` | 复制来源模板 ID（可选） |
| `createdAt` | 创建时间 |

### projectItems[] 元素

| 字段 | 说明 |
|------|------|
| `id` / `name` | 价目项目 |
| `unlimited` | 是否不限次 |
| `purchaseQty` / `giftQty` | 购买/赠送次数 |
| `qtyScope` | `per_item`（每项）\| `shared`（共计） |
| `sharedGroupId` | 共计组 ID（共计时） |

### 折扣 memberPrices

- 比例：`{ tickIndex: N }`（对应标尺档，展示为 N 折/免费/原价）  
- 或固定：`{ mode: 'fixed', amount: '299' }`  

## 7.2 持卡实例（示意）

| 字段 | 说明 |
|------|------|
| 会员 ID、模板 ID | 关联 |
| 办卡实付 | 用于退卡/业绩分摊 |
| 有效期至 / 是否永久 | |
| 面值余额拆分（购买/赠送） | 退卡明细 |
| 项目已用/剩余（购买/赠送） | |
| 折扣已用次数与已省金额 | 策略相关 |

账本结构 `【待联调确认】` 与现网会员资产表对齐；语义须支撑模块 8 退卡计算。

## 7.3 接口能力清单

1. 模板列表（支持在售/已下架筛选）  
2. 模板详情（含运营统计、卡面信息、策略）  
3. 创建模板 / 更新模板（更新须校验无有效持卡） / 复制模板  
4. 下架 / 重新上架  
5. 价目项目列表（供权益勾选）`【待联调确认】`  
6. 可办卡会员列表（排除已持该卡）/ 已持卡会员列表  
7. 办卡（支持批量会员）  
8. 延期  
9. 退卡估值查询 + 确认退卡（提交实际退款金额）  
10. 运营统计查询  

## 7.4 谁在什么时机写数据

| 时机 | 动作 |
|------|------|
| Step3 保存（create/duplicate） | 创建模板，默认在售 |
| Step3 保存（edit） | 覆盖模板（服务端校验无有效持卡） |
| 确认下架/上架 | 更新 `shelved` |
| 办卡确定 | 写持卡实例；更新统计 |
| 确认延期 | 更新持卡有效期；可记参考费用 |
| 确认退卡 | 结束持卡；记实退金额与审计 |

## 7.5 时序（示意）

```
创建：Step1→Step2→Step3 SaveTemplate → CreateSuccess → optional IssueCard
编辑：Detail →（无持卡）Steps → UpdateTemplate → Detail
复制：Duplicate → Steps → SaveTemplate(new) → CreateSuccess
办卡：IssueMembers → IssueCard → IssueSuccess
退卡：Holders → RefundQuote → ConfirmRefund
```

## 7.6 联调待确认

- [ ] 模板/持卡 API 路径与字段命名  
- [ ] 购买金额与支付实付在办卡时的取值来源  
- [ ] 参考费用是否生成收款单  
- [ ] 实退是否走退款通道及幂等  
- [ ] 「管理项目表」入口权限与路由  
- [ ] `audience` 是否本期暴露 UI  

---

# 模块 8　业务规则与算例
> 读者：前端 ✓ · 后端 **必看** · 测试 ✓

## 8.1 赠送面值

当开启面值权益：

\[
赠送 = \max(0,\ 面值金额 - 购买金额)
\]

（展示与存贮格式与原型一致，通常为金额字符串/两位小数。）

## 8.2 项目计次

| 模式 | 规则 |
|------|------|
| 每项 `per_item` | 每个适用项目各自拥有相同的购买/赠送次数或各自不限次 |
| 共计 `shared` | 多个项目共享一个次数池；消耗任一项目扣同一池 |
| 不限次 | 不可选共计；卡有效期 **不能为永久** |

## 8.3 用卡策略公式（配置期文案）

**消耗及退卡 · 均值 `alloc_pool`**

- 一般：`消耗价值 = 项目面值 ×（办卡实付金额 ÷ 总权益面值）`  
- 纯折扣 / 特殊卡型：按原型 hint 与明细弹窗（如按有效期天数比例等）实现，须与估值接口一致。

**消耗及退卡 · 减值 `retail`**

- `消耗价值 = 项目面值`（按已消耗权益的原价累计）

**业绩 · 均值 `alloc_pool`**

- `员工业绩 = 项目面值 ×（办卡实付 ÷ 总权益面值）`  
- 不限次项目可附加：劳动业绩按价目表原价计（原型 hint）

**业绩 · 原价 `retail`**

- `员工业绩 = 项目面值`

详情展示标签：均值退卡 / 减值退卡 · 均值计绩 / 减值计绩。

## 8.4 整卡退卡建议退款（均值池示意）

原型均值退卡逻辑概要：

1. 取办卡实付 `purchase`  
2. 按账本累计已消耗价值 `consumed`（余额消耗、项目次数×分摊或原价、折扣已省等，细则与 `refundRule` 一致）  
3. `建议退款 = max(0, purchase - consumed)`  
4. 界面展示分项明细；**实际退款**默认建议值，店主可改后提交  

减值模式按「项目面值」口径累计消耗（`computeSuggestedRefundRetail` 语义）。

**无分项可估时**：提示按办卡实付协商退款。

## 8.5 办卡与持卡约束

- 同一会员对同一模板：未退卡前不可再次新办（办卡列表排除已持卡）。  
- 已下架模板拒绝新办。

## 8.6 算例（测试可对照）

**算例 A · 纯面值**  
购买 1000，面值 1000，赠送 0。未消费。均值/减值建议退款均接近 1000（无消耗）。

**算例 B · 项目每项**  
购买 500；项目「剪发」购买 10 次，单次面值 50；已用 2 次。  
- 减值：消耗 ≈ 2×50 = 100；建议退款 ≈ 400（若实付 500 且无其他权益）。  
- 均值：消耗 = 已用部分对应分摊价值（实付/总面值×已用面值）；建议退款 = 实付 − 消耗。

**算例 C · 不限次**  
不可永久有效；退卡消耗按策略与有效期/原型明细计算。

（联调时用接口返回值与上表口径对齐；若现网公式有常量系数，以联调纪要更新本节。）

---

# 模块 9　文案清单与权限
> 读者：前端 ✓ · 后端 部分 · 测试 ✓

## 9.1 文案清单（摘自原型，须一致）

| 位置 | 文案 |
|------|------|
| 列表标题 | 会员卡管理 |
| Tab | 在售 / 已下架 |
| 底栏 | 添加会员卡 |
| 空·在售 | 暂无会员卡 / 点击底部「添加会员卡」开始创建 |
| 空·下架 | 暂无已下架会员卡 / 下架后的卡会出现在这里，仍可查看详情、复制建卡或管理已持卡 |
| 列表操作 | +复制建卡 / 办卡/退卡 |
| Step1 标题 | 添加会员卡 / 编辑会员卡 |
| 步骤条 | 第 1/3 步 · 基本信息；第 2/3 步 · 权益组合；第 3/3 步 · 用卡策略 |
| 字段 | 卡名称* / 购买金额* / 有效期限* / 卡面模板 |
| 单位 | 日 / 月 / 年 / 永久 |
| 色名 | 品牌红 / 金棕 / 深蓝 / 紫罗兰 / 青绿 |
| Step2 | 权益组合；面值；项目；折扣；+添加项目权益；+添加折扣权益 |
| 面值描述 | 购买与赠送的面值，永久有效。 |
| 项目描述 | 包含、赠送项目的次数 |
| 折扣描述 | 比例或定额折扣 |
| Step3 | 用卡策略；消耗及退卡计算规则；业绩计算规则；均值模式；减值模式；原价模式 |
| 保存 | 保存 / 保存修改 / 下一步 |
| 复制横幅 | 正在基于「{名}」创建新卡，保存后原卡不受影响 |
| 创建成功 | 会员卡已创建；已上架，可立即为会员办理；返回列表；立即为会员办卡 |
| 详情 | 会员卡详情；下架；编辑；复制建卡；重新上架；已持卡管理 |
| 运营数据 | 办卡总额；办卡次数；办卡人数；持卡余额；当前有效；消费次数（含赠送）；剩余次数（含赠送） |
| 下架框 | 下架会员卡；确定下架「{名}」？；下架后新客将无法办理该卡；已持有该卡的客户不受影响。；确认下架 |
| 上架框 | 重新上架；确定重新上架「{名}」？；上架后新客可再次办理该卡；已持卡客户不受影响。；确认上架 |
| 选会员 | 选择会员；办卡；退卡/延期；搜索姓名或手机；确定；退卡；延期；确认延期 |
| 退卡 | 退卡估值；办卡实付；消耗价值；建议退款；实际退款；取消；确认退卡 |
| 办卡成功 | 办卡成功；返回列表；继续办卡 |
| 计次 | 每项；共计；权益次数；赠送次数；不限次；适用项目；全选；反选 |
| 折扣 | 打折；固定金额；免费；原价 |
| Toast | 见模块 5.3 与：已下架；已重新上架；已保存；已添加/已保存项目权益；已添加/已保存折扣权益；办卡/延期/退卡成功句式 |

## 9.2 权限

- **默认**：店主可进行本文全部操作。  
- **店员**：另议（原型未区分）。后端预留鉴权扩展。  

---

# 模块 10　验收标准 / 造数场景
> 读者：前端 了解 · 后端 了解 · 测试 **必看**

## 10.1 P0 场景表（Given → When → Then）

| # | 场景 | 前置 | 操作 | 期望 |
|---|------|------|------|------|
| 1 | 空态建卡 | 在售无卡 | 添加→三步合法保存 | 创建成功；列表出现在售卡 |
| 2 | Step2 无权益 | Step1 已填 | 权益全关点下一步 | toast「请至少选一项权益内容」 |
| 3 | 面值赠送 | 购买 800 面值 1000 | 看赠送 | 赠送显示 200 |
| 4 | 项目每项 | 选 2 项目各 10 次 | 保存 ticket | 摘要为每项·10次 |
| 5 | 共计 | 选 2 项目共计 10 次 | 保存 | 摘要共计·项目名；共享池 |
| 6 | 不限次禁永久 | 期限永久 | 开不限次 | 弹窗要求改非永久期限 |
| 7 | 策略保存 | Step3 选减值+原价 | 保存 | 详情显示减值退卡·减值计绩 |
| 8 | 办卡 | 在售卡、有未持卡会员 | 办卡多选确定 | 办卡成功；统计增加 |
| 9 | 重复办卡 | 会员已持卡 | 打开办卡 Tab | 该会员不在可选列表 |
| 10 | 锁编辑 | 有效持卡>0 | 点编辑 | 禁用或 toast 引导复制 |
| 11 | 复制 | 任意卡 | 复制建卡保存 | 新卡；原卡不变 |
| 12 | 下架 | 在售卡 | 确认下架 | 进入已下架；不可新办 |
| 13 | 上架 | 已下架 | 确认上架 | 回在售；可办卡 |
| 14 | 延期 | 非永久持卡 | 设时长确认 | toast 延长成功 |
| 15 | 永久勿延 | 永久持卡 | 点延期 | toast 无需延期 |
| 16 | 退卡 | 有持卡 | 估值改实退确认 | 退卡成功；不再有效持卡 |
| 17 | 已下架持卡 | 已下架模板 | 已持卡管理 | 仅退卡/延期 Tab |

## 10.2 回归

- 编辑无持卡模板后详情数据与卡面一致。  
- 下架后再上架，办卡恢复。  
- 折扣固定金额与打折互斥按页内模式切换。  
- 退卡取消不改变持卡。  

## 10.3 原型造数场景（测试对照交互原型）

| 场景 | 如何得到 |
|------|----------|
| 全空列表 | 原型侧栏「无数据」或清空模板 |
| 有数据 | 原型侧栏「有数据」：10 张演示卡 + 18 价目项目 |
| 在售详情 | 打开任一在售卡详情 |
| 已下架详情 | 打开已下架演示卡 |
| 创建 Step1–3 | 功能链路或「添加会员卡」 |
| 选项目/折扣页 | Step2 进入添加权益 |
| 办卡/退卡 Sheet | 列表「办卡/退卡」或详情已持卡管理 |

正式环境用测试账号造同等数据，不必依赖演示按钮。

---

# 模块 11　非功能（极简）
> 读者：前端 ✓ · 后端 ✓ · 测试 ✓

| 项 | 要求 |
|------|------|
| 提交 | 保存/办卡/退卡防重复点击；失败可重试 |
| 列表 | 模板量增大时保持可滚动、可接受性能 |
| 兼容 | 目标机型与现网商户 App WebView 一致 |
| 埋点 | 本期不强制事件表；若现网有统一埋点规范则联调时对齐 |

---

# 模块 12　交付物
> 读者：前端 ✓ · 后端 — · 测试 ✓

| 交付物 | 说明 |
|--------|------|
| 本 PRD（Markdown） | `PRD-会员卡管理.md`（给 AI / 研发的源文件） |
| 本 PRD（阅读预览） | `PRD-会员卡管理.html`（表格与标题排版预览；非第二份规格） |
| 交互原型 | `demo.html`（或同内容中文文件名镜像） |

**开发与测试只需要以上两样。** 视觉、文案、流程以原型为准；规则、数据、验收以本 PRD 为准。二者冲突时：**先以原型界面为准修正 PRD，再开工**（本文发布时已按原型对齐）。

画布：390 × 844。品牌色：`#F32F41`。

---

## 附录 A · 卡面颜色

| key | 名称 |
|-----|------|
| brand_red | 品牌红（默认） |
| gold | 金棕 |
| blue | 深蓝 |
| purple | 紫罗兰 |
| teal | 青绿 |

## 附录 B · 依赖说明

- **价目项目**：项目/折扣权益勾选依赖门店项目数据；项目管理产品本身不在本期交付。  
- **会员**：办卡依赖既存会员列表；会员档案管理不在本期。  
- **支付/退款通道**：办卡收款、延期参考费用、实退退款 `【待联调确认】`。
